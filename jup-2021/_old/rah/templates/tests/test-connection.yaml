apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rah.fullname" . }}-test-connection"
  labels:
    {{- include "rah.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rah.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
